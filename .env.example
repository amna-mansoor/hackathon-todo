# [Task]: T007
# [From]: speckit.plan / Phase 1: Project Setup & Monorepo Configuration
# [Spec]: speckit.specify / TR-005

# --- Backend Environment Variables ---
DATABASE_URL="postgresql+psycopg2://user:password@host:port/database"
# Example for Neon Serverless PostgreSQL:
# DATABASE_URL="postgresql+psycopg2://[user]:[password]@[endpoint_hostname]/[db_name]?sslmode=require"

SECRET_KEY="your-super-secret-key"
# Used for JWT encoding/decoding. Must be a strong, randomly generated string.
# Example: openssl rand -hex 32

ALGORITHM="HS256"
# Algorithm used for JWT signing.

ACCESS_TOKEN_EXPIRE_MINUTES=30
# Expiration time for JWT access tokens in minutes.

# --- Frontend Environment Variables (for Next.js) ---
# These might be defined in .env.local in the frontend directory
# NEXTAUTH_URL="http://localhost:3000"
# NEXTAUTH_SECRET="your-nextauth-secret"
# NEXT_PUBLIC_API_BASE_URL="http://localhost:8000"
